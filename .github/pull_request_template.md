## Summary
- Briefly describe what this PR changes.

## Motivation / Context
- Why is this change needed?
- Link related issue(s) if available.

## Changes
- List key implementation changes.
- Mention affected crates/modules.

## Testing
- Commands run:
  - `cargo fmt --all -- --check`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
- Include notable test output or manual validation notes.

## Checklist
- [ ] Code is formatted (`cargo fmt --all -- --check`).
- [ ] Lints pass (`cargo clippy --workspace --all-targets --all-features -- -D warnings`).
- [ ] Tests pass (`cargo test --workspace`).
- [ ] Documentation/comments updated where relevant.
- [ ] Breaking changes are documented (or `N/A`).
